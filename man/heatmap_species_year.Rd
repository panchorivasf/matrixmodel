% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmap_species_year.R
\name{heatmap_species_year}
\alias{heatmap_species_year}
\title{Create interactive heatmap of species by year}
\usage{
heatmap_species_year(
  plot_id,
  data = NULL,
  dir = out_dir,
  metric = c("BA_total", "N_total"),
  save_html = FALSE
)
}
\arguments{
\item{plot_id}{Character or numeric identifier for the plot to visualize}

\item{data}{Either a data frame or character string specifying file
path/pattern.
  If character, uses `latest_file()` to find the most recent matching file.}

\item{dir}{Character string specifying directory path to search for files
(only used if `data` is a character string).}

\item{metric}{Character string specifying the metric to plot: "BA_total"
(basal area)
 or "N_total" (tree density). Defaults to "BA_total".}

\item{save_html}{Logical indicating whether to save interactive HTML plot.
Defaults to FALSE.}
}
\value{
A plotly heatmap object showing species metrics across years
}
\description{
Generates an interactive heatmap showing species-level metrics (basal area
or tree density) across years for a specific plot. Accepts either a data
frame or file path.
}
\examples{
\dontrun{
# From data frame
hm <- heatmap_species_year_interactive(plot_id = "P1", data = species_data)

# From file path
hm <- heatmap_species_year_interactive(plot_id = "P1", data =
"plot_species_year_data.csv")

# From directory with pattern matching
hm <- heatmap_species_year_interactive(plot_id = "P1", dir = "output",
                                      data = "plot_species_year_.*\\\\.csv")
}

}
