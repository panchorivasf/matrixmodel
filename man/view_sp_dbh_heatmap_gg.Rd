% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/view_sp_dbh_heatmap_gg.R
\name{view_sp_dbh_heatmap_gg}
\alias{view_sp_dbh_heatmap_gg}
\title{Create static heatmap of species by DGP for single or multiple years using ggplot2}
\usage{
view_sp_dbh_heatmap_gg(
  plot_id = NULL,
  year = 50,
  data = NULL,
  metric = c("basal", "density"),
  all_years = FALSE,
  dark = TRUE,
  save_plot = FALSE,
  save_path = NULL,
  save_grid = FALSE,
  save_animation = FALSE
)
}
\arguments{
\item{plot_id}{Character or numeric identifier for the plot to visualize.
If NULL, aggregates data across all plots.}

\item{year}{Numeric value for the year to visualize. If NULL and all_years = TRUE,
creates plots for all available years.}

\item{data}{Either a data frame or character string specifying file path/pattern.
If character, uses `read_csv()` to read the file.}

\item{metric}{Character string specifying the metric to plot: "basal" (basal area)
or "density" (tree density). Defaults to "basal".}

\item{all_years}{Logical. If TRUE, creates plots for all available years.
Requires year = NULL. Defaults to FALSE.}

\item{dark}{Logical. Whether to use a dark theme for the graph.}

\item{save_plot}{Logical indicating whether to save the plot as PNG.
Defaults to FALSE.}

\item{save_path}{Character string specifying directory path to save the plot.}

\item{save_grid}{Logical. Whether to save a grid plot with all individual years.
Only used when all_years = TRUE. Defaults to FALSE.}

\item{save_animation}{Logical. Whether to create and save an animation GIF.
Only used when all_years = TRUE. Defaults to FALSE.}
}
\value{
A ggplot heatmap or a list of plots depending on parameters
}
\description{
Generates static heatmaps showing species-level metrics by DGP
for specific year(s) and plot, with appropriate scaling.
}
\examples{
\dontrun{
# Single year plot
plot <- view_sp_dbh_heatmap_gg(plot_id = "P1", year = 50, data = spcd_dgp_data)

# All years with grid and animation
plots <- view_sp_dbh_heatmap_gg(plot_id = "P1", year = NULL, data = spcd_dgp_data,
                              all_years = TRUE, save_grid = TRUE, save_animation = TRUE)
}

}
