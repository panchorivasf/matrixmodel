% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_species_trends.R
\name{plot_species_trends}
\alias{plot_species_trends}
\title{Plot species trends over time using plotly}
\usage{
plot_species_trends(
  plot_id,
  data = NULL,
  save_to = NULL,
  metric = c("BA_total", "N_total"),
  top_n = 9,
  save_html = FALSE
)
}
\arguments{
\item{plot_id}{Character or numeric identifier for the plot to visualize}

\item{data}{Either a data frame or character string specifying file path/pattern.
If character, uses `latest_file()` to find the most recent matching file.}

\item{save_to}{Character string specifying directory path to search for files
(only used if `data` is a character string).}

\item{metric}{Character string specifying the metric to plot: "BA_total" (basal area)
or "N_total" (tree density). Defaults to "BA_total".}

\item{top_n}{Integer specifying number of top species to display. Defaults to 9.}

\item{save_html}{Logical indicating whether to save interactive HTML plot.
Defaults to FALSE.}
}
\value{
A plotly object showing species trends over time for the specified plot
}
\description{
Creates interactive line plots showing species-level trends (basal area or tree density)
over time for a specific plot. Accepts either a data frame or file path.
}
\examples{
\dontrun{
# From data frame
p <- plot_species_trends(plot_id = "P1", data = species_data)

# From file path
p <- plot_species_trends(plot_id = "P1",
data = "plot_species_year_data.csv")

# From directory with pattern matching
p <- plot_species_trends(plot_id = "P1", save_to = "output",
                        data = "plot_species_year_.*\\\\.csv")
}

}
