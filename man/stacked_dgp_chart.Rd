% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stacked_dgp_chart.R
\name{stacked_dgp_chart}
\alias{stacked_dgp_chart}
\title{Create interactive stacked area chart of DGP metrics}
\usage{
stacked_dgp_chart(
  plot_id,
  data = NULL,
  save_to = NULL,
  metric = c("BA_total", "N_total"),
  save_html = FALSE
)
}
\arguments{
\item{plot_id}{Character or numeric identifier for the plot to visualize}

\item{data}{Either a data frame or character string specifying file path/pattern.
If character, uses `latest_file()` to find the most recent matching file.}

\item{save_to}{Character string specifying directory path to search for files
(only used if `data` is a character string).}

\item{metric}{Character string specifying the metric to plot: "BA_total" (basal area)
or "N_total" (tree density). Defaults to "BA_total".}

\item{save_html}{Logical indicating whether to save interactive HTML plot.
Defaults to FALSE.}
}
\value{
A plotly stacked area chart showing DGP metrics over time
}
\description{
Generates an interactive stacked area chart showing diameter growth proportion (DGP)
metrics (basal area or tree density) over time for a specific plot.
}
\examples{
\dontrun{
# From data frame
chart <- stacked_dgp_chart(plot_id = "P1", data = dgp_data)

# From file path
chart <- stacked_dgp_chart(plot_id = "P1", data = "plot_dgp_year_data.csv")

# From directory with pattern matching
chart <- stacked_dgp_chart(plot_id = "P1", save_to = "output",
                          data = "plot_dgp_year_.*\\\\.csv")
}

}
