% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/species_dgp_grid.R
\name{species_dgp_grid}
\alias{species_dgp_grid}
\title{Create interactive faceted heatmap grid of species by DGP}
\usage{
species_dgp_grid(
  plot_id,
  years = 50,
  data = NULL,
  dir = out_dir,
  metric = c("BA_total", "N_total"),
  save_html = FALSE
)
}
\arguments{
\item{plot_id}{Character or numeric identifier for the plot to visualize}

\item{years}{Numeric vector of years to include in the faceted grid}

\item{data}{Either a data frame or character string specifying file path/pattern.
If character, uses `latest_file()` to find the most recent matching file.}

\item{dir}{Character string specifying directory path to search for files
(only used if `data` is a character string).}

\item{metric}{Character string specifying the metric to plot: "BA_total" (basal area)
or "N_total" (tree density). Defaults to "BA_total".}

\item{save_html}{Logical indicating whether to save interactive HTML plot.
Defaults to FALSE.}
}
\value{
A plotly subplot grid showing species by DGP heatmaps across years
}
\description{
Generates an interactive grid of heatmaps showing species-level metrics by DGP
across multiple years for a specific plot.
}
\examples{
\dontrun{
# From data frame
grid <- species_dgp_grid(plot_id = "P1", years = c(0, 50, 100), data = spcd_dgp_data)

# From file path
grid <- species_dgp_grid(plot_id = "P1", data = "plot_spcd_dgp_year_data.csv")

# From directory with pattern matching
grid <- species_dgp_grid(plot_id = "P1", dir = "output",
                        data = "plot_spcd_dgp_year_.*\\\\.csv")
}

}
